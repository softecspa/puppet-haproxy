if $syslogfacility-text == 'mail' and ( <% addresses.split(',').each do |address| %>$msg contains '<%= address -%>' or <% end -%> $msg contains 'Unexpected SSL connection shutdown' ) then <%= logdir -%>/<%= log_file %>
&~
